services:
  - type: web
    name: wealth-wars-game
    runtime: node
    plan: free
    rootDir: WealthWars-Deploy 3
    buildCommand: npm run build
    startCommand: npm start
    healthCheckPath: /health
    autoDeployTrigger: commit
    envVars:
      - key: NODE_ENV
        value: production
