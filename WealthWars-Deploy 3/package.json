{
  "name": "wealth-wars-deploy",
  "version": "2.5.0",
  "private": true,
  "description": "Single-service deployment bundle for the Wealth Wars multiplayer game",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "build": "npm ci --include=dev --prefix frontend && npm ci --include=dev --prefix backend && npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm start --prefix backend",
    "test": "npm test --prefix backend -- --runInBand",
    "verify": "npm run build && npm run test"
  }
}
